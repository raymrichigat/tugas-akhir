# Python
__pycache__/
*.pyc
.env
venv/
.ipynb_checkpoints/
*.egg-info/
dist/
build/

# Claude Code state (lokal, jangan commit)
.claude/
CLAUDE_1.md

# Model artifacts (terlalu besar untuk GitHub, share via Drive/HF Hub)
*.safetensors
*.bin
*.ckpt
*.pt
*.pth
src/pseudo_labelling/SRL-NER/done_running/**/output/models/

# PDF referensi (kecuali proposal sendiri & buku sumber yang sudah tracked)
*.pdf
!Proposal-TA Genta Putra Prayoga.pdf
!data/Sirah Nabawiyah by Shafiyyur Rahman Mubarakfury (syaikh.), Kathur Suhardi.pdf

# Paket original Bu Diana + artifact zip Mac
(KIRIM MHS Bu Diana)_bert-only-0.9/
__MACOSX/

# OS
.DS_Store
Thumbs.db

# Personal / draft TA — jangan di-commit
Buku-TA-Genta.docx
instruksi.txt
*.docx

# Backup files dari script (idempotent backup .bak sebelum overwrite)
*.csv.bak
*.csv.bak2
*.csv.bak*
*.md.bak*

# Bundle data sementara untuk upload Colab (regeneratable)
corrected_gold_data_*.zip
inference_bundle_augmentation_v4.zip
inference_bundle_augmentation_v4/

# Training outputs lokal hasil run (model + config + tokenizer, total belasan GB — share via Drive/HF)
data/result/pseudo-labelling/SRL-NER/done_running/

# Zip download run done_newest (bundle model+artefak dari Colab/Drive, GB-an, regeneratable)
# Catatan: report .md kecil di done_newest tetap boleh di-commit (di-add manual).
data/result/pseudo-labelling/SRL-NER/done_newest/zip_file/
data/result/pseudo-labelling/SRL-NER/done_newest/**/*.zip

# Backup & bundle data lokal (redundant dgn file gold yang sudah tracked, regeneratable)
data/result/_verify_backup_*/
data/result/pseudo-labelling/SRL-NER/corrected_gold_data_*/

# Folder sampah/arsip hasil cleanup lokal (122MB, regeneratable/arsip — jangan commit)
lama/sampah/

# Bundle training (zip + folder unzip, regeneratable, duplikat CSV yang sudah tracked)
training_bundle_corrected_gold_*

# Presentasi (binary besar, share via Drive — sama spt *.docx/*.pdf)
*.pptx

# Draw.io file backup/lock sementara (autosave editor, bukan sumber)
.$*
*.bkp
